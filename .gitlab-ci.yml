stages:
  - build
  - deploy

#####################################
# 🚀 Étape 1 : Build & Push Docker Image
#####################################
build:
  stage: build
  image: docker:20.10.16  # Utilisation d'une image avec Docker CLI
  services:
    - name: docker:dind
      alias: docker
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: "tcp://docker:2375"
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: "overlay2"
    NODE_ENV: "production"
  before_script:
    - echo "⏳ Attente du démarrage de Docker DIND..."
    - until docker info >/dev/null 2>&1; do echo "⏳ Docker DIND n'est pas encore prêt..."; sleep 3; done
    - echo "✅ Docker DIND est prêt."
  script:
    - echo "🐳 Construction et push de l'image Docker..."
    - docker build -t docker.io/$DOCKERHUB_USERNAME/$DOCKER_IMAGE_NAME:latest -f Dockerfile.prod .
    - docker login -u "$DOCKERHUB_USERNAME" -p "$DOCKERHUB_PASSWORD"
    - docker push docker.io/$DOCKERHUB_USERNAME/$DOCKER_IMAGE_NAME:latest
  only:
    - main

#####################################
# 🚀 Étape 2 : Déploiement sur le serveur
#####################################
deploy:
  stage: deploy
  image: docker:20.10.16  # Utilisation d'une image avec Docker CLI
  variables:
    DOCKER_HOST: "unix:///var/run/docker.sock"  # Permet d'utiliser Docker de l'hôte
  before_script:
    - echo "🛠 Déploiement local sur le serveur (Docker tourne directement sur l'hôte)."
  script:
    - echo "🔄 Connexion à Docker Hub..."
    - docker login -u "$DOCKERHUB_USERNAME" -p "$DOCKERHUB_PASSWORD"
    - echo "📥 Téléchargement de la dernière image..."
    - docker pull docker.io/$DOCKERHUB_USERNAME/$DOCKER_IMAGE_NAME:latest
    - echo "🛑 Arrêt et suppression du conteneur existant..."
    - docker stop nuxt-container || true
    - docker rm nuxt-container || true
    - echo "🚀 Lancement du nouveau conteneur..."
    - docker run -d --restart=always -p 3000:3000 --name nuxt-container docker.io/$DOCKERHUB_USERNAME/$DOCKER_IMAGE_NAME:latest
    - echo "✅ Déploiement terminé !"
  only:
    - main
