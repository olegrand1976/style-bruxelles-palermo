#####################################
# Étape 2 : Déploiement sur le serveur
#####################################
deploy:
  stage: deploy
  image: docker:20.10.16
  services:
    - name: docker:dind
      alias: docker
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: overlay2
  before_script:
    - apk add --no-cache openssh-client sshpass
    - echo "🛠 Vérification de la connexion SSH..."
    - ssh -i id_rsa -o UserKnownHostsFile=known_hosts -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "echo '✅ Connexion SSH OK'"
  script:
    - echo "🛠 Connexion au serveur distant et exécution des commandes Docker..."
    - >
      ssh -i id_rsa -o UserKnownHostsFile=known_hosts -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "
        echo '🔄 Connexion à Docker Hub...' &&
        docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD &&
        echo '📥 Téléchargement de la dernière image...' &&
        docker pull docker.io/$DOCKERHUB_USERNAME/$DOCKER_IMAGE_NAME:latest &&
        echo '🛑 Arrêt et suppression du conteneur existant...' &&
        docker stop nuxt-container || true &&
        docker rm nuxt-container || true &&
        echo '🚀 Lancement du nouveau conteneur...' &&
        docker run -d --restart=always -p 3000:3000 --name nuxt-container docker.io/$DOCKERHUB_USERNAME/$DOCKER_IMAGE_NAME:latest &&
        echo '✅ Déploiement terminé !' &&
        docker ps -a
      "
  only:
    - main